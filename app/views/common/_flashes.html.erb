<% if flash.any? %>
  <% user_facing_flashes.each do |key, value| %>
    <div class="alert alert-dismissable alert-<%= flash_class(key) %> fade in">
      <button class="close" data-dismiss="alert" aria-label="close">×</button>
      <%= value %>
    </div>
  <% end %>
<% end %>
